#include <stdio.h>
#include <canio.h>
void main()
{
    int year;
    printf("enter a year");
    scanf("%d",&year);
    if(year % 4==0)
{
    printf("%d is a leap year,"year);
}
     else
{
    printf("%d is noy a leap year,"year);
}
}